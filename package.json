{
  "name": "toktickit",
  "private": true,
  "version": "1.0.0",
  "description": "TokTickIT - root-level tooling (Playwright E2E/visual tests). The app itself lives in client/ and server/.",
  "scripts": {
    "e2e": "playwright test",
    "e2e:ui": "playwright test --ui",
    "e2e:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
